apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ArgoCD bootstrap configuration
# Deploys ArgoCD root application and all managed applications
resources:
  - root-application.yaml
  - applications/

labels:
  - pairs:
      managed-by: bootstrap
      component: argocd
