apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Bootstrap components applied in order
# CRDs must be applied first, then other components
resources:
- bootstrap/crds/
- bootstrap/security/
