apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kairos operational resources - managed by the kairos-ops ArgoCD Application (manual-sync)
# NodeOp and NodeOpUpgrade resources are NOT continuously reconciled; they are applied
# on-demand by triggering a manual sync in ArgoCD.
resources:
  - configure-kube-apiserver-nodeop.yaml
  - upgrades/active/
