apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: default
  namespace: metallb-system
spec:
  # Optionally restrict announcement to specific pools and/or interfaces.
  ipAddressPools:
    - default
