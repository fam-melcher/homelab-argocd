apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Intentionally empty until you set a site-specific address pool.
# See ops/metallb/README.md.
resources: []

labels:
  - pairs:
      managed-by: argocd
      component: metallb-config
