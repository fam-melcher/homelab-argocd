apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ipaddresspool.yaml
  - l2advertisement.yaml

labels:
  - pairs:
      managed-by: argocd
      component: metallb-config
