apiVersion: v1
kind: Pod
metadata:
  name: sleeper
  namespace: alpine-test
spec:
  containers:
    - name: sleep
      image: alpine:latest
      command: ["sleep", "86400"]
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
        limits:
          cpu: 50m
          memory: 64Mi
