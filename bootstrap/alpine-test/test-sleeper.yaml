apiVersion: v1
kind: Pod
metadata:
  name: sleeper
  namespace: alpine-test
  labels:
    app: sleeper
spec:
  containers:
    - name: sleep
      image: alpine:latest
      command: ["sleep", "86400"]
