apiVersion: apps/v1
kind: Deployment
metadata:
  name: alpine-sleep
spec:
  template:
    spec:
      containers:
      - name: alpine
        lifecycle:
          postStart:
            exec:
              command: [ "/bin/sh", "-c", "apk add --no-cache htop" ]
