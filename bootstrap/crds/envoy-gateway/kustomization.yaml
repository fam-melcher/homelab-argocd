apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - envoy-gateway-crds.yaml

labels:
  - pairs:
      managed-by: argocd
      component: crds
      crd-group: envoy-gateway
