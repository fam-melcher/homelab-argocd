apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kairos operator CRDs
# Must be applied before using NodeOp resources
  resources:
  - nodeop-crd.yaml

labels:
  managed-by: argocd
  component: crds
