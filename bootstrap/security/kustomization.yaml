apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Security bootstrap resources
# Note: Pod security is now managed as a Kairos NodeOp in ops/kairos/k3s/
resources: []

labels:
  - pairs:
      managed-by: argocd
      component: security
